# macOS CI policy (MS-013.3)

**Recommended** (not Must): Generated Core GHA Default is Linux-only.
macOS CI jobs are Recommended for distribution / mac binary smoke when the
`distribution` profile is selected. Windows remains forbidden (product lock).
