# Example Project Specs

Fixtures for `foundry validate` / `foundry plan` (PHASE-01+).

| File | Purpose |
| ---- | ------- |
| `minimal-cli.toml` | Valid minimal `cli` + empty profiles (MS-003/MS-004 golden target) |

Do not invent fields or profile IDs outside the closed catalog / revised spec.
