id = "distribution"
kind = "profile"
description = "Optional cargo-dist profile (no Windows targets)"
requires = ["core", "cli"]

[[files]]
path = "dist-workspace.toml"
template = "templates/dist-workspace.toml"

[[files]]
path = ".github/workflows/release.yml"
template = "templates/release.yml"